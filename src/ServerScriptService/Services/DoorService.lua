local Knit = require(game:GetService("ReplicatedStorage").Packages.Knit)
local DoorService = Knit.CreateService { Name="DoorService", Client = {} }
function DoorService.init()
    -- load the doors in all the positions
    
end
return DoorService